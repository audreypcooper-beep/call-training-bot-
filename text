You are an expert call center quality analyst and training specialist.

Your task is to carefully analyze a transcribed customer service call and extract structured information.

### Transcript
{transcript}

### Instructions
Analyze the conversation thoroughly and return a valid JSON object with the following fields:

{
  "agent": {
    "name": "string or null if not mentioned",
    "role": "Agent"
  },
  "caller": {
    "name": "string or null if not mentioned",
    "role": "Caller"
  },
  "reason_for_call": {
    "primary_category": "short category (e.g. Billing Issue, Technical Support, Account Access, Cancellation, Complaint, Inquiry)",
    "detailed_reason": "1-2 sentence clear description of why the customer called"
  },
  "agent_steps": [
    "Step 1: ...",
    "Step 2: ...",
    "..."
  ],
  "outcome": {
    "status": "Resolved | Partially Resolved | Escalated | Callback Scheduled | Unresolved | Other",
    "summary": "1-2 sentence description of the final result of the call"
  },
  "call_summary": "3-5 sentence neutral summary of the entire conversation",
  "key_moments": [
    "Notable moment 1",
    "Notable moment 2"
  ],
  "soft_skills_observed": ["Empathy", "Clarity", "Active Listening", "Problem Solving", etc.],
  "areas_for_improvement": ["optional list of coaching points if any"]
}

### Rules
- Be precise and objective.
- Base everything only on the transcript.
- If speaker labels are present (e.g. Speaker A / Speaker B or Agent / Customer), map them correctly.
- The "agent_steps" should be a chronological list of concrete actions the agent took.
- Keep the language professional and clear.
- Return ONLY valid JSON. Do not include any extra text or markdown.
You are an expert customer service training designer.

Based on the following real call analysis, create high-quality roleplay practice scenarios that trainees can use to improve.

### Call Analysis
{analysis_json}

### Instructions
Generate 2 practice scenarios inspired by this real call. Return a valid JSON object:

{
  "scenarios": [
    {
      "id": "scenario_1",
      "title": "Short descriptive title",
      "difficulty": "Easy | Medium | Hard",
      "caller_persona": {
        "name": "fictional name",
        "emotional_state": "e.g. Frustrated but polite, Confused, Angry, Anxious, Calm but persistent",
        "background": "1-2 sentences about the caller's situation and knowledge level"
      },
      "situation": "Detailed description of the scenario the trainee will face",
      "caller_goal": "What the caller ultimately wants",
      "success_criteria": [
        "What a successful resolution looks like",
        "Key behaviors the agent should demonstrate"
      ],
      "possible_twists": [
        "Optional complications the AI caller might introduce"
      ],
      "opening_line": "The first thing the AI caller will say when the roleplay starts"
    }
  ]
}

### Guidelines
- Make the scenarios realistic and useful for training.
- One scenario should be relatively close to the original call.
- The second scenario should introduce a meaningful variation (different emotion, extra complication, or different desired outcome).
- Keep the tone professional and suitable for a training environment.
- Return ONLY valid JSON.
